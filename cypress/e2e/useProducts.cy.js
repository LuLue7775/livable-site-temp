describe('useProduct fetches firebase data with react-query', () => {
  it('should return products', () => {
    // instead of cy.intercept(), we use cypress-firebase.
    // to test useQuery https://www.youtube.com/watch?v=KXlMayH6j9w
  })
})
